# Build output (regenerated by deploy.bat / vite build)
deploy/

# Raw source material — too large for git
transfer/
audio/
temp/
old/

# gstack tooling state
.gstack/

# personal Claude Code settings (per-user, not for the repo)
.claude/settings.local.json
